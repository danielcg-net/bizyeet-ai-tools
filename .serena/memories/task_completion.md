# Task completion

- Run `npm run check` before committing.
- Confirm no secrets or private BizYeet material is staged.
- For workflow changes, ensure `npm run check:workflows` passes and actions remain full-SHA pinned.
- Use a pull request; protected `main` is never pushed directly.